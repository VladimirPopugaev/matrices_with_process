На данный момент:

1. Сделать так, чтоб каждый раз создавался процесс (file_worker), который в качестве состояния использует имя матрицы. Он занимается сохранением и выдачей обратно матриц.

а) при инициализации регистрировать процесс как :worker_name, где name - это имя матрицы (на данный момент не обращаем внимание, что могут совпдать имена)
б) При сохранении и выдаче передавать только data, потому что имя будет храниться как состояние матрицы

2. Главный сервер должен хранить в себе имена и айдишники (посмотреть как лучше) всех процессов file_worker.  
